version: 2
updates:
  # keep the GitHub Actions used in workflows up to date
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly

  # keep the optional Node bridge's dependencies up to date
  - package-ecosystem: npm
    directory: "/server"
    schedule:
      interval: weekly
