{
  "name": "claude-browser-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Local bridge: runs the Claude agent loop and drives the Claude Helper Chrome extension.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
